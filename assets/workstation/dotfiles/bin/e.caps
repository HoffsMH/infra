#!/bin/bash

source $HOME/.envrc

if [ -f "$CAP_FILE" ]; then
  ~/bin/internals/cap.sync
fi

~/bin/internals/cap.dedup-links

rg "$1" -l $CAP_DIR | tail -n 20 | sort -h | hcat > $CAP_FILE

~/bin/internals/nvim.cap
