#!/bin/bash

capfile=$(t $(echo "$HOME/personal/00-capture/time/work.md" | prefix name))
selection=$(select-input $@)

echo  "$selection" >> $capfile

notify-send "work: ${selection}"

echo $capfile

