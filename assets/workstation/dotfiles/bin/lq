#!/bin/bash


# Create a temporary file
tempfile=$(mktemp)

nvim -c "$" "$tempfile"

cat "$tempfile" | mods --continue $(date-str) -m 4 -P "see attached file:"


# Clean up the temp file
rm "$tempfile"
