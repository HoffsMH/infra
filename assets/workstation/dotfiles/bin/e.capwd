#!/bin/bash

source $HOME/.envrc

if [ -f "$CAP_FILE" ]; then
  ~/bin/internals/cap.sync
fi

~/bin/internals/cap.dedup-links

fd "$(~/bin/internals/cap.get-wd)" $CAP_DIR | sort | tail -n 20 | hcat > $CAP_FILE

~/bin/internals/nvim.cap
