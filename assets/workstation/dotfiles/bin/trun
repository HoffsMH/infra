#!/bin/zsh

if [[ $# -gt 0 ]];
then
  echo "trying to run"
  echo "'$@'"
  tmux new-window -n "$(basename $(pwd)) $@"  "run-wa '$@'"
else
  cmd=$(fho)

  input=${cmd/trun /}
  input=${input/\"/}
  input=${input/\"/}

  trun "$input"
fi
