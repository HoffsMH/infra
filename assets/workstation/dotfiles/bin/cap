#!/bin/bash

source $HOME/.envrc

newcapfile=$(t $(echo "$HOME/personal/00-capture/time/cap.md" | prefix name))

selection=$(select-input $@)

echo "$selection" | append_title >> $newcapfile

cap.dedup-links

notify-send "cap: ${selection}"

echo $newcapfile

