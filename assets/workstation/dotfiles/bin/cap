#!/bin/bash

source $HOME/.envrc

newcapfile=$(~/bin/internals/cap.new $1)

selection=$(~/bin/internals/select-input $@)

echo "$selection" | append-html-title >> $newcapfile

~/bin/internals/cap.dedup-links

notify-send "cap: ${selection}"

echo $newcapfile

