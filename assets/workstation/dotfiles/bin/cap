#!/bin/bash

source $HOME/.envrc

newcapfile=$(cap.new)

selection=$(select-input $@)

echo "$selection" | append_title >> $newcapfile

cap.dedup-links

notify-send "cap: ${selection}"

echo $newcapfile

