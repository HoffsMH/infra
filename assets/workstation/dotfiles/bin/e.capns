#!/bin/bash

source $HOME/.envrc

if [ -f "$CAP_FILE" ]; then
  ~/bin/internals/cap.sync
fi

~/internals/cap.dedup-links

rg "$1" --files-without-match $CAP_DIR | sort | tail -n 20 | hcat > $CAP_FILE

~/bin/internals/nvim.cap
