#!/bin/bash

source $HOME/.envrc

if [ -f "$CAP_FILE" ]; then
  exit 1
fi

cap.dedup-links

rg "$1" --files-without-match $CAP_DIR | tfilter "${@:2}" | sort -h | hcat > $CAP_FILE

nvim -c $ $CAP_FILE

cap.sync
