#!/bin/bash

handle_sigint() {
    echo "Interrupt received, stopping..."
    exit 0
}

trap handle_sigint SIGINT

if [ $# -eq 0 ]; then
    echo "Usage: $0 <command> [arguments...]"
    exit 1
fi

while true; do
    "$@"
done
